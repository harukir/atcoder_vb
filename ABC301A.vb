''

'Imports System
'Imports System.IO
'Imports System.Runtime.CompilerServices
'Imports System.Security.Cryptography.X509Certificates
'Imports System.Text

'Public Class pg


'    Shared Sub Main()

'        Dim N As Decimal = Decimal.Parse(Console.ReadLine())
'        Dim S As String = Console.ReadLine()

'        Dim t As Decimal = S.Replace("A", "").Length
'        Dim a As Decimal = S.Length - t

'        Dim lastW As String = S.Substring(S.Length - 1, 1)

'        Dim win As String = If(t > a, "T", "A")

'        If t = a Then win = If(lastW = "T", "A", "T")

'        Console.WriteLine(win)


'    End Sub

'End Class
