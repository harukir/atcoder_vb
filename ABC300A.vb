''

'Imports System
'Imports System.IO
'Imports System.Runtime.CompilerServices
'Imports System.Security.Cryptography.X509Certificates
'Imports System.Text

'Public Class pg


'    Shared Sub Main()

'        Dim arg As String() = Console.ReadLine().Split(" ")
'        Dim N As Decimal = Decimal.Parse(arg(0))
'        Dim A As Decimal = Decimal.Parse(arg(1))
'        Dim B As Decimal = Decimal.Parse(arg(2))

'        arg = Console.ReadLine().Split(" ")

'        For i As Decimal = 0 To N - 1


'            If (A + B) = Decimal.Parse(arg(i)) Then
'                Console.WriteLine((i + 1).ToString)
'                Return
'            End If
'        Next


'    End Sub

'End Class
